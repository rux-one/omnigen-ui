# Backend port (must match BACKEND_PORT in backend environment)
BACKEND_PORT=5000

# Frontend port
PORT=8080

# API base URL (constructed using BACKEND_PORT)
VUE_APP_API_BASE_URL=http://localhost:${BACKEND_PORT}/api
